services:
  api:
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPOSITORY}:latest
    ports:
      - "8000:8000"
    networks:
      - ml-net

networks:
  ml-net:
    driver: bridge